DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'proxy-service',
        'USER': 'root',
        'PASSWORD': 'root',
        'HOST': 'mysql',
    }
}