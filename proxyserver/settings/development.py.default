DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'proxy_server',
        'USER': 'root',
        'PASSWORD': 'root',
        'HOST': 'localhost',
    }
}